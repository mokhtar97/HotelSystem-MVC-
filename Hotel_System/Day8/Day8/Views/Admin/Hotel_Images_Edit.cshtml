@model Day8.Models.Hotel_Images

@{
    ViewBag.Title = "Hotel_Images_Edit";
    Layout = "~/Views/Shared/_LayoutAdminpanel.cshtml";
}

<h2>Hotel_Images_Edit</h2>


@using (Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Hotel_Images</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)

    <div class="form-group">
        image: <input type="file" name="file" required="" value=@Model.Image>
    </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.hotelID, "hotelID", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("hotelID", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.hotelID, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    
}

<div>
    @Html.ActionLink("Back to List", "Hotel_Images_Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
